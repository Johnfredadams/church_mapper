<%= form_for(@renew_region) do |f| %>
  <% if @renew_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@renew_region.errors.count, "error") %> prohibited this renew_region from being saved:</h2>

      <ul>
      <% @renew_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit class:"form-control" %>
  </div>
<% end %>
